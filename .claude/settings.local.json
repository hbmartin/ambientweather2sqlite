{
	"permissions": {
		"allow": [
			"Bash(uv run pytest tests/test_database_manager.py -v)",
			"Bash(source:*)",
			"Bash(uv run:*)",
			"Bash(uv run pytest tests/test_server.py -v)",
			"Bash(uv run pytest tests/test_server.py::TestServer::test_server_integration_hourly_data_supports_legacy_date_query -v -s)"
		],
		"deny": []
	},
	"hooks": {
		"PostToolUse": [
			{
				"matcher": "Edit|Write",
				"hooks": [
					{
						"type": "command",
						"command": "uv run black ."
					}
				]
			}
		]
	}
}
